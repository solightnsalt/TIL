### 리뷰

+ 장기 교육과정이 다 몰입형..

  화이팅 🥳

+ 이론적으로 점검

+ 실제문제랑 연결성 높이기
+ 아직 파이썬 3일차야! 바로바로 풀리는게 이상한거
+ 개념적으로 잡혀있는 것을 코드로 구현하는 것 
+ 원래 에러나는게 당연한겨 그걸 어케 풀까 풀릴때까지 고민하는게 즐거워져야한다~! 
+ 우선은 코드업 다 풀고 백준 단계별 1~6정도까지 풀어보기

---

### 쉅 전,, 

#### 데이터 구조 (Data structure)

+ 뒤에 쩜찍어서 쓰던거 뭐지 ? 
+ Imperative knowledge 

#### 변수와 타입 

+ ​		타입 : int. Float, complex etc 

#### 함수 

+ 인풋이 이렇게 들어오면 아웃풋 이렇게 내줄게~~ 
+ [1, 2, 3].append(4) -> *데이터타입.메써드() 있다*

#### 메서드(methods)

---

### 시퀀스

+ 타입.메서드()
  1. 타입 누가 ? 
  2. (input)
  3. Output 

> a.sort() ? Sorted(a) ?
>
> 1. 리스트메서드 
>    + 원본을 변경. 리턴 none
>    
>      ```python
>      a = [10, 1, 100]
>      a.sort()
>      ```
>
> 2. 리스트에 함수 이용
>
>    + 원본 변경 안함. 정렬된 리스트를 리턴 
>
>      ```python
>      a = [10, 1, 100]
>      b = sorted(b)
>      ```

#### 문자열 

+ '', "" 로 표기되는 문자열의 나열 

##### 문자열 탐색/검증 

+ s.find(x)
  + x의 첫번째 위치를 반환, 없으면 -1 
  + 'word'.find('w') 는 0이래 ㅠㅠ 인덱스는 0없댜 
+ s.index(x)
  + x 첫번째 위치 반환. 없으면 오류!!! 
+ s.islpha 알파벳으로 이뤄져잇냐
+ s.isupper
+ s.islower
+ 이런게 있구나 생각ㅎ 



##### 문자열 관련 검증 메서드 

+ 문자열.is\_\_\_\_() 

  + 치환하지 않고 문자열이 is 뒤 규칙에 맞는지 판단해서 true/false 반환

    > Ex
    >
    > ```python
    > s = 'python'
    > s.islower()
    > 
    > True
    > ```



##### 문자열 변경 

+ .replace(old, new[,count]
  + 바꿀 대상글자를 새로운 글자로 바꿔서 반환, 
  + Count 해당 개수만큼 실행 
  
+ .strip([chars])
  + 특정 문자 제거하거나 공백 제거, 개행이랑 스페이스 포함 
  + 양쪽( 보통 이거만 씀), lstrip 왼, rstrip 오 
  
+ split()

  + ()기준으로 문자열 쪼개서 리스트로 반환 

+ 'separator'.join([iterable])

  + 반복가능한 컨테이너요소들을 구분자로 합쳐서 문자열 반환

  + ' '. join(map(str, [1, 2, 3]))

  + 바로 위처럼 하는 이요? **string만 되기때문 !**

    > `map(str, [1, 2, 3])`
    >
    > 은 str을 한 결과를 [] 안의 요소 하나하나에 적용시키라는 뜻 

+ 기타드등등 

+ 문자는 imuutable 한 점 잘 기억 

+ > 리플레이스 
  >
  > 스플릿 
  >
  > 세퍼레이터
  >
  > 조인 
  >
  > 정도만 실제 코딩시 사용하고나머지는 잘 안쓴다고 하시넹  



#### 리스트 

+ 변경 가능한 값들의 나열된 자료형 .
+ 반복 가능 
  + append
  + 꼭꼭 알고 있기 
  + 리스트에 값을 추가함 
+ Insert(i. x)
  + 정해진 위치에  추가

+ pop

  + 정해진 위치에 잇는 값 삭제, 그 학목 반환

+ Clear  안에 있는거 다 지움 . 

+ sort

  + 원본리스트 정령, 반환 안함 
  + Sorted 함수랑 분간

+ count

  + 원하는 값의 개수
  + 리스트를 순회하며 (for) 값이 1인 것을 count+1=1ㅋ

+ Insert 

+ remove ? 

+ s.vo 는 메서드 sum() 함수 

+ 와 도라방스 진짜 왜케 졸리냐 커피도 졸라 많이 마셨는데

+ s = [1, 2, 3]

+ result()

+ List string 

  힝구 너무 잠와 

  일어나 제발ㅠㅠㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋ 쳐도른자아녀 

  잠이오냐고 ㅠ 

  

  **인덱스 순서 왜 아직 모르겠냐고 .... 0부터 시작한다면서 왜 아닌데 ㅠ **

### 세트 메서드

는 이런게 있습니다 정도로 알아두기 



### 딕셔너리 

+ 키 - 값 
+ Fruit = {'apple' : '사과', 'banana' : '바나나'}
  + Fruite['apple'] 이 방식은 무조건 에러난다 



+ print(my_dict.get('apple', 0))



#### 추가 및 삭제 

+ .pop(key[.default])
+ get pop 
+ .update 
+ 딕셔너리의 기본 반복문
+ 기본순회 
  + key가 기준, 직접 key로 접그낳면 value를 얻을 수 있다 ~! 
  + Di

+ **Key value items** 

+ 대체 왜 이러는건데 
+ `my_dict_2[a]='airplane'
+ my_dict_3['a'] = {'a' : 0}
+ My_dict_3['a'] = my_dict_3[] 
+ 거의 뭐 두시간 존거 아니냐고 도라이야ㅠㅠ 점심도 먹지말고 되돌려보기해라.. 



+ 딕셔너리는 인덱스 개념 없고 키로만 호출할 수 있음 
+ my







### Dictionary

+ 값을 가지고 올 때 {대괄호} 사용 
+ .

